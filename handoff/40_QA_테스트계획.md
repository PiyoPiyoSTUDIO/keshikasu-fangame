# 40. QA 테스트 계획

- **문서 버전**: v1.1
- **최종 수정**: 2026-07-09
- **범위**: 코어 루프 · 데이터/서버 · L10N · 성능 · 코드 규칙 검증.

---

## 1. 목적
릴리즈 전, Phase 1 머지가 모든 지원 환경 · 5개 언어에서 정상 동작함을 보장.

> **구현 현황(2026-07-09, Cycle 5b)**
> - **통과 확인**: F1~F7(코어 루프) · S1·S2(로컬 최고점) · S5·S6(only-if-best 전송) · U1(로딩) · U3(START→게임) · **U6·U7(벽지 티커)** — 수동 확인 완료
> - **구현됐으나 정식 검증 전**: S3·S4·S10(오프라인/익명세션/서버장애 graceful)
> - **미구현**: U2 일부(랭킹·언어 버튼은 자리만) · U4(랭킹 표시) · U5(언어 전환) · S7·S8(이름 입력/변경) · S9(랭킹 조회 표시) · L1~L6(현재 ja 고정)
> - 상세 `15_구현현황`

## 2. 테스트 환경 매트릭스
| 우선순위 | 환경 |
|---|---|
| 필수 | iOS Safari (아이폰), Android Chrome |
| 보조 | 데스크톱 Chrome/Edge (개발 확인용) |
| 화면 | 좁은~넓은 세로 비율 (노치/세이프에어리어 포함) |

## 3. 화면 테스트 (로딩 / 타이틀 / 벽지)
| # | 케이스 | 기대 결과 | 상태 |
|---|---|---|---|
| U1 | 로딩 화면 | 핑크(#fce0e3) 배경 + 프로그레스 표시 | ✅ |
| U2 | 타이틀 화면 | 로고 · START · 랭킹 · 언어선택 · 최고점 표시 | △ (랭킹·언어는 자리만) |
| U3 | START | GameScene 진입 | ✅ |
| U4 | 랭킹 버튼 | 랭킹 조회 표시 | ⏳ 미구현 |
| U5 | 언어 선택 | 5개 언어 즉시 전환(`25`) | ⏳ 미구현 |
| **U6** | **벽지 사선 티커 (넓은 화면)** | 캔버스 양옆 레터박스에 사선 띠가 **끊김 없이** 흐름. 한 줄 걸러 방향 반대. 상하 여백 없음 | ✅ |
| **U7** | **벽지 입력 통과** | 캔버스 **가장자리** 탭/드래그가 정상 동작(티커가 입력을 가로채지 않음) | ✅ |
| **U8** | **벽지 모바일 세로** | 여백이 거의 없어 티커는 사실상 비노출. **레이아웃 무붕괴 · 스크롤바 없음** | ✅ |
| **U9** | **`prefers-reduced-motion`** | OS "동작 줄이기" ON 시 티커 애니메이션 정지, 레이아웃은 유지 | ⏳ 미검증 |

## 4. 기능 테스트 (코어 루프)
| # | 케이스 | 기대 결과 |
|---|---|---|
| F1 | 드롭 위치 지정 후 낙하 | 지정 가로 위치에서 낙하, 물리 정지 |
| F2 | 같은 종류 접촉 | 합체 → 상위 1단 생성, 점수 가산 |
| F3 | 다른 종류 접촉 | 합체 안 됨, 각자 적재 |
| F4 | 드롭 쿨다운 | 낙하 직후 중복 드롭 차단 |
| F5 | 데드라인 초과 지속 | 게임오버 발동 |
| F6 | 다시하기 | 판 초기화, 점수 0 |
| F7 | NEXT 미리보기 | 다음 드롭과 실제 일치 |

## 5. 합체 사다리 테스트
- 11단 전부 합체로 도달 가능한지(1→…→11 マンモス)
- 드롭 가능 단계가 1~3단으로 제한되는지
- 최종 단계(11) 합체 시 처리(보너스/소멸 등 Vol.2 정의 전까지 크래시 없음)

## 6. 데이터 / 서버 테스트
| # | 케이스 | 기대 결과 | 상태 |
|---|---|---|---|
| S1 | 최고점 갱신 | localStorage 즉시 저장 | ✅ |
| S2 | 재실행 | 최고점 복구 | ✅ |
| S3 | 오프라인 플레이 | 서버 없이 정상 동작 | ⏳ |
| S4 | 익명 세션 | 최초 1회 자동 생성(화면 X), 재실행 복원 | ⏳ |
| S5 | 로컬 최고점 미갱신 | 서버 전송 안 함(egress 절약) | ✅ |
| S6 | 로컬 최고점 갱신 | 서버 upsert(본인 1행, 중복 행 X) | ✅ |
| S7 | 이름 최초 입력 | 1회 입력 → localStorage 기억 | ⏳ 미구현 |
| S8 | 이름 변경(설정) | 다음 갱신부터 반영 | ⏳ 미구현 |
| S9 | 랭킹 조회 | best 내림차순 상위 N | ⏳ UI 미구현 |
| S10 | 서버 장애/오프라인 | graceful 스킵, 다음 갱신 시 재전송 | ⏳ |

> **S6 주의(회귀 포인트)**: "최고점 갱신 여부" 판정은 반드시 **저장된 이전 best**(`loadLocalBest()`)와 비교해야 한다. 플레이 중 실시간 갱신되는 `this.best`와 비교하면 게임오버 시점엔 항상 `best === score`가 되어 판정이 무너진다.

## 7. L10N 테스트 (★ 다국어)
| # | 케이스 | 기대 결과 |
|---|---|---|
| L1 | 5개 언어 전환 | 모든 표시 텍스트 해당 언어로 |
| L2 | 미지원 기기 언어 | `ja`로 폴백 |
| L3 | 미번역 키 | `ja` 폴백, 앱 안 깨짐 |
| L4 | 긴 문자열(en/zh) | 레이아웃 안 깨짐(말줄임/줄바꿈) |
| L5 | 폰트 렌더 | ja/ko/zh-CN/zh-TW 자형 정상 |
| L6 | 언어별 글자 이미지 | 로케일에 맞는 에셋 로드 |
| **L7** | **벽지 티커 4언어 자형** | ja/en/ko/zh 문자가 한 줄 안에서 깨짐(두부/□) 없이 렌더 (`25` §13) |

> **L5/L6 시점에 중국어 검수 일괄 수행** (`25` §12). 벽지 문구 `#可愛卡薯`는 자사 공식 로고 유래라 검수 대상 제외.

### 7.1 미번역 키 검출 스크립트
```javascript
// jaのキー集合と各言語のキー集合を比較し、未翻訳キーを洗い出す
const fs = require('fs');
const load = (l) => Object.keys(JSON.parse(fs.readFileSync(`./src/locales/${l}.json`)));
const ja = load('ja');                              // 基準キー
for (const l of ['en', 'ko', 'zh-CN', 'zh-TW']) {
  const have = new Set(load(l));
  const missing = ja.filter((k) => !have.has(k));   // jaにあって当該言語に無いキー
  console.log(l, 'missing:', missing.length, missing);
}
```

## 8. 성능 테스트
- 모바일에서 오브젝트 다수(20~40개) 적재 시 프레임 유지(체감 끊김 없음)
- **벽지 티커 상시 애니메이션(띠 30개)이 프레임에 영향을 주지 않는지** — `will-change: transform`로 GPU 합성 유도. 저사양 기기 확인 필요
- 메모리 누수(장시간 플레이) 확인
- 초기 로딩 시간(에셋/폰트) 허용 범위

## 9. 코드 규칙 검증
### 9.1 한글-금지 (코드 로직)
```python
import re, glob
# コードロジック(ts/sql/yaml/sh)にハングルが混入していないか検査する
# ※ locales/*.json は翻訳データのため対象外
# ※ index.html の data-words も表示データのため対象外（.htmlはそもそも走査しない）
HANGUL = re.compile(r'[\uac00-\ud7a3\u3130-\u318f]')
CODE = ('.ts', '.sql', '.yaml', '.yml', '.sh')
bad = 0
for f in glob.glob('src/**/*', recursive=True):
    if not f.endswith(CODE):
        continue
    for i, line in enumerate(open(f, encoding='utf-8'), 1):
        if HANGUL.search(line):
            print(f, i, line.strip()); bad += 1
print('violations:', bad)   # 0でなければNG
```
- 기대: violations 0
- **화이트리스트(검사 제외)**: `src/locales/*.json`(번역 데이터), `index.html`의 `data-words`(벽지 표시 데이터). 상세 `25` §11
- **주의**: `.html`을 `CODE` 튜플에 추가하지 말 것. 추가하면 `#케시카스`가 오탐된다. 단 `index.html` 내부의 **주석·로직**은 사람 눈으로 일본어 룰을 확인한다.

## 10. 릴리즈 전 회귀 체크리스트
- [ ] U1~U9 통과
- [ ] F1~F7 통과
- [ ] S1~S10 통과
- [ ] L1~L7 통과
- [ ] 성능 OK (벽지 티커 포함)
- [ ] 한글-금지 검사 0건
- [ ] 중국어 검수 완료(`25` §12)
- [ ] 효과음 라이선스/크레딧 확인
- [ ] 빌드/배포 스모크 테스트(`60`)

## 11. 미해결
- 부정 점수(클라 신뢰 한계) 대응 — Phase 1 보류
- 자동화 테스트 도입 범위(현재 수동 중심)
- 기기 실측 디바이스 목록 확정
- U9(`prefers-reduced-motion`) 실제 검증 방법: Chrome DevTools → Rendering → Emulate CSS media feature
