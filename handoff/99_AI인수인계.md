# 99. AI 인수인계 (Claude 컨텍스트 연속성)

- **문서 버전**: v1.0
- **최종 수정**: 2026-06-24
- **목적**: Claude의 컨텍스트가 초기화되어도, 이 문서만 읽으면 프로젝트 상태 · 결정 · 규칙을 즉시 이어받을 수 있게 한다.
- **갱신 주기**: 매 작업 사이클 종료 시.

---

## 1. 프로젝트 한 줄 요약
케시카스(Piyopiyo Studio 자체 IP) 팬게임. 웹 우선 모바일. 1차는 드롭 & 합체 머지. 무료 스택. 판매 아님.

## 2. 확정 결정 로그
| 항목 | 결정 | 비고 |
|---|---|---|
| 배포 | 웹(PWA) 우선 → 차후 Capacitor 앱 | |
| 장르 순서 | 머지 → 클리커 → 수집 + 미니게임 | 확장성 고려 |
| 엔진 | Phaser 4.1 | 2026 정식, Matter 물리 내장 |
| 언어 | TypeScript | C# 감각 전이 |
| 빌드 | Vite | |
| 호스팅 | GitHub Pages | |
| 백엔드 | Supabase Free | 랭킹 + 클라우드세이브 |
| Supabase 조직 | Piyopiyo Studio | |
| Supabase 프로젝트 | keshikasu-fangame | 리전 Tokyo (Northeast Asia) |
| 무료 한도 | 활성 프로젝트 2개 / 계정 (현재 1개 사용 → 여유 1) | 7일 미사용 시 정지, 핑으로 해결 예정 |
| 다국어(L10N) | ja / en / ko / zh-CN / zh-TW (5개) | 폴백 = ja. 모든 텍스트·이미지 키 처리 |
| 배경색 | `#fce0e3` (파스텔 핑크) | 전 화면 공통 (config·manifest 반영) |
| 랭킹 방식 | 익명인증 백킹 (가) + only-if-best 전송 | 로그인 화면 X, 기기당 1행 |
| 이름 UX | 최초 1회 입력 → localStorage 기억 → 설정서 변경 | 기본값 'ゲスト' |
| 로딩/타이틀 | 핑크 배경 + 캐릭터 바운스(절차적), START·랭킹·언어·최고점 | 기존 일러스트 활용, 일부 분리 |

## 3. 현재 상태
- **완료**: 문서 세트 9종 전부 (`00` · `10`Vol.1 · `20` · `25` · `30` · `40` · `50` · `60` · `99`)
- **진행 예정**: `data/merge_ladder.ts`(11단 구성 확정 후) → 동의 받고 Cycle 1 코드
- **코드**: 아직 0줄 (전 문서 · L10N · 에셋/QA/빌드 설계 확정)
- **사다리**: 11단 확정. 구성 = **올-동물 11종**(벌→…→マンモス, GDD §5 초안), 크기 순서 타쿠 확정 대기

## 4. 작업 규칙 (반드시 준수)

### 4.1 언어 룰
- **코드 주석: 일본어 무조건** (한국어 단어 금지). 예) 「後ほど」「予定」「変更」「修正」「将来」.
  - ※ 이 규칙은 **코드 파일** 한정. 본 기획 / 사양 문서는 의도적으로 한국어로 작성.
  - ※ **예외**: `locales/*.json`(번역 데이터)의 값은 각 언어 문자 정상. 한글-금지는 코드 로직·주석(ts/sql/yaml/sh)에만 적용. (`25` §11)
- **대화(사용자): 한국어**
- **프롬프트(외부 LLM 호출): 영어**
- 코드 작성 후: 한글 자음(ㄱ-ㅎ) 검색 → 0건 확인.

### 4.2 5원칙
1. 가정 X (코드 안 보고 추측 X, 필요 시 파일 요청)
2. 최소 코드 (필요한 곳만 수정)
3. 필요한 곳만 (영향 범위 파악 후)
4. 성공 기준 정의 후 검증
5. 위치 명확화 (앞뒤 기존 코드 함께 제시)

### 4.3 커뮤니케이션
- 트레이드오프 ≠ 작업 지시. 코딩 착수 전 **명시적 동의 필수**.
- 한 번에 한 단계. 작은 사이클 ("하나하나 완성").
- 할 일은 `[할 일 N]`으로 분리. 각 항목에 명령 / 위치 / 기대결과 / 분기 포함.
- 파일명 대소문자 정확히.

## 5. 용어집
- **케시카스**: Piyopiyo Studio 자체 IP. 음식 · 사물 약 30종 + 동물 약 8종.
- **머지(수박게임형)**: 같은 것끼리 합쳐 상위 단계로 키우는 메커닉.
- **합체 사다리(merge ladder)**: 합체 단계 순서표. 데이터로 분리.
- **PWA**: 설치형 웹앱. **Capacitor**: 웹 → 네이티브 래퍼.
- **MAU**: 월 활성 사용자. **RLS**: Supabase 행 단위 보안 정책.
- **데드라인**: 머지 플레이필드 상단 경계. 넘으면 게임오버.
- **L10N / i18n**: 다국어화. 표시 문자열을 키로 처리해 언어별 테이블에서 조회.
- **로케일(locale)**: 언어 코드(ja · en · ko · zh-CN · zh-TW). 폴백 = ja.

## 6. 미결 과제
- 합체 사다리 최종 확정 (크기 · 단계 수) — 타쿠
- Supabase 조직 / 프로젝트 실제 생성 — 타쿠 (진행 중)
- `20_TDD` · `50_서버사양` 작성 — Claude
- 학습자료 갭 유의: Phaser 4는 신버전이라 웹 예제 다수가 v3 기준 (API 거의 호환)

## 7. 다음 단계
- 타쿠: GDD §5 11단 구성/순서 확정 + Supabase 환경 생성
- Claude: 사다리 확정 후 `data/merge_ladder.ts` 작성 → 명시적 동의 받고 Cycle 1 코드(빈 Phaser 화면 + 오브젝트 1개 낙하)

## 8. 갱신 규칙
- 매 사이클 종료 시 "2. 결정 로그 / 3. 현재 상태 / 6. 미결 / 7. 다음 단계" 갱신.
