# 99. AI 인수인계 (Claude 컨텍스트 연속성)

- **문서 버전**: v1.1
- **최종 수정**: 2026-06-25
- **목적**: Claude의 컨텍스트가 초기화되어도, 이 문서만 읽으면 프로젝트 상태 · 결정 · 규칙을 즉시 이어받을 수 있게 한다.
- **갱신 주기**: 매 작업 사이클 종료 시.

---

## 1. 프로젝트 한 줄 요약
케시카스(Piyopiyo Studio 자체 IP) 팬게임. 웹 우선 모바일. 1차는 드롭 & 합체 머지. 무료 스택. 판매 아님.

## 2. 확정 결정 로그
| 항목 | 결정 | 비고 |
|---|---|---|
| 배포 | 웹(PWA) 우선 → 차후 Capacitor 앱 | |
| 장르 순서 | 머지 → 클리커 → 수집 + 미니게임 | 확장성 고려 |
| 엔진 | Phaser 4.2 | 2026 정식, Matter 물리 내장 |
| 언어 | TypeScript | C# 감각 전이 |
| 빌드 | Vite | |
| 호스팅 | GitHub Pages | |
| 백엔드 | Supabase Free | 랭킹 + 클라우드세이브 |
| Supabase 조직 | Piyopiyo Studio | |
| Supabase 프로젝트 | keshikasu-fangame | 리전 Tokyo (Northeast Asia) |
| 무료 한도 | 활성 프로젝트 2개 / 계정 (현재 1개 사용 → 여유 1) | 7일 미사용 시 정지, 핑으로 해결 예정 |
| 다국어(L10N) | ja / en / ko / zh-CN / zh-TW (5개) | 폴백 = ja. 모든 텍스트·이미지 키 처리 |
| 배경색 | `#fce0e3` (파스텔 핑크) | 전 화면 공통 (config·manifest 반영) |
| 랭킹 방식 | 익명인증 백킹 (가) + only-if-best 전송 | 로그인 화면 X, 기기당 1행 |
| 이름 UX | 최초 1회 입력 → localStorage 기억 → 설정서 변경 | 기본값 'ゲスト' |
| 로딩/타이틀 | 핑크 배경 + 캐릭터 바운스(절차적), START·랭킹·언어·최고점 | 기존 일러스트 활용, 일부 분리 |
| 설치 버전 | Phaser 4.2.0 / Vite 8.1.0 / TypeScript 5.9.3 | 2026-06-24 npm install, lockfile 고정 |
| 배포 URL | https://piyopiyostudio.github.io/keshikasu-fangame/ | GitHub Pages 자동배포(deploy.yml) |

## 3. 현재 상태
- **완료**: 문서 + **Cycle 3** + 캐릭터 11종 + GitHub Pages 배포
  - Cycle 2 = 머지 코어 플레이 가능
  - **Cycle 3 = 드롭 x 클램프 버그 수정 + ⑥ L10N 적용(ja 고정) + ① juice(착지 스쿼시·합체 파티클·효과음 3종)**
- **빌드 동작**: 드롭 · 클램프 · 쿨타임(600) · NEXT · 합체 · 점수 · 팝 · 스쿼시 · 파티클 · 효과음 · 게임오버 · 다시하기. **GDD Vol.1 성공기준 달성**. 상세·튜닝값 → `15_구현현황`
- **L10N 상태**: i18n 엔진(`services/i18n.ts`) + 5개 언어 사전(`locales/*.json`, 15키) + 표시문자열 키화 완료. 단 **현재 ja 고정**(`setDicts(ja, ja)`). 언어선택 UI·detectLocale 연결·타 언어 import는 ④ 타이틀 사이클로 미룸. 중국어 캐릭터명 검수 미완(`25` §12)
- **코드 위치**: 회사 `C:\Users\woo_s\keshikasu-fangame` / 집 `C:\Users\TAKU\keshikasu-fangame`. GitHub `PiyoPiyoSTUDIO/keshikasu-fangame` (토큰 인증, 양 PC 동기화 — 열 때 pull / 닫을 때 push)
- **에셋**: `にこにこ.pdf`(벡터)에서 11종 추출. **매머드 `+`뱃지는 원본 디자인이라 유지**. 도감용 나머지 26종은 Phase 3. 효과음 wav 3종은 무료 음원(CC0 가정 — 라이선스 종류 미확정, 출시 전 크레딧 검토)
- **미구현**: 로컬 최고점, Supabase 랭킹, 타이틀 화면, 밸런스 튜닝, L10N 언어선택 UI

## 4. 작업 규칙 (반드시 준수)

### 4.1 언어 룰
- **코드 주석: 일본어 무조건** (한국어 단어 금지). 예) 「後ほど」「予定」「変更」「修正」「将来」.
  - ※ 이 규칙은 **코드 파일** 한정. 본 기획 / 사양 문서는 의도적으로 한국어로 작성.
  - ※ **예외**: `locales/*.json`(번역 데이터)의 값은 각 언어 문자 정상. 한글-금지는 코드 로직·주석(ts/sql/yaml/sh)에만 적용. (`25` §11)
- **대화(사용자): 한국어**
- **프롬프트(외부 LLM 호출): 영어**
- 코드 작성 후: 한글 자음(ㄱ-ㅎ) 검색 → 0건 확인.

### 4.2 5원칙
1. 가정 X (코드 안 보고 추측 X, 필요 시 파일 요청)
2. 최소 코드 (필요한 곳만 수정)
3. 필요한 곳만 (영향 범위 파악 후)
4. 성공 기준 정의 후 검증
5. 위치 명확화 (앞뒤 기존 코드 함께 제시)

### 4.3 커뮤니케이션
- 트레이드오프 ≠ 작업 지시. 코딩 착수 전 **명시적 동의 필수**.
- 한 번에 한 단계. 작은 사이클 ("하나하나 완성").
- 할 일은 `[할 일 N]`으로 분리. 각 항목에 명령 / 위치 / 기대결과 / 분기 포함.
- 파일명 대소문자 정확히.

## 5. 용어집
- **케시카스**: Piyopiyo Studio 자체 IP. 음식 · 사물 약 30종 + 동물 약 8종.
- **머지(수박게임형)**: 같은 것끼리 합쳐 상위 단계로 키우는 메커닉.
- **합체 사다리(merge ladder)**: 합체 단계 순서표. 데이터로 분리.
- **PWA**: 설치형 웹앱. **Capacitor**: 웹 → 네이티브 래퍼.
- **MAU**: 월 활성 사용자. **RLS**: Supabase 행 단위 보안 정책.
- **데드라인**: 머지 플레이필드 상단 경계. 넘으면 게임오버.
- **L10N / i18n**: 다국어화. 표시 문자열을 키로 처리해 언어별 테이블에서 조회.
- **로케일(locale)**: 언어 코드(ja · en · ko · zh-CN · zh-TW). 폴백 = ja.

## 6. 미결 과제
- 밸런스 튜닝: 1~2단 크기차가 작음 (`merge_ladder` radius 조정)
- 합체 사다리 4~11단 실제 등장/난이도 검증
- L10N 언어선택 UI 미구현: 현재 ja 고정. ④ 타이틀에서 detectLocale 연결 + 5개 언어 import + 선택 UI 추가 예정
- 중국어 캐릭터명 검수 미완 (zh-CN/zh-TW 초안 상태, `25` §12)
- 효과음 라이선스 종류 미확정 (CC0 가정 — 출시 전 크레딧 표기 필요 여부 확인)
- 타이틀 화면(TitleScene)·로컬 최고점·Supabase 랭킹 미구현
- 학습자료 갭 유의: Phaser 4 일부 API가 v3와 다름 (예: 바디 목록은 `world.getAllBodies()`)

## 7. 다음 단계
- 작업 순서 확정: **⑥ L10N → ① juice → ② 로컬 최고점 → ③ Supabase 랭킹 → ④ 타이틀 → ⑤ 밸런스**
- **완료**: ⑥ L10N(ja 고정) · ① juice(스쿼시·파티클·효과음)
- **다음 = ② 로컬 최고점 저장(localStorage)**. 시작 전 `services/saveLocal.ts` 존재 여부 확인부터(가정 X). 있으면 내용 보고, 없으면 신규 생성. `50` §7.1의 only-if-best 흐름이 localBest를 참조하므로 ③의 선행
- 남은 순서: **②→③→④⑤**. ④ 타이틀에서 L10N 언어선택 UI도 같이 처리(detectLocale 연결·5개 언어 import)
- 방향 정해지면 작은 사이클(파일 하나씩)로 진행

## 8. 갱신 규칙
- 매 사이클 종료 시 "2. 결정 로그 / 3. 현재 상태 / 6. 미결 / 7. 다음 단계" 갱신.
